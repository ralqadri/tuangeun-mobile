import 'package:flutter/material.dart';
import 'resto_class.dart';
import 'card_resto-info.dart';
import 'page_restoinformation.dart';

Widget buildRatingCommentCard(Resto resto, int index, BuildContext context) {
  return Container(
    width: MediaQuery.of(context).size.width,
    height: 250,
    padding: const EdgeInsets.all(24),
    margin: const EdgeInsets.symmetric(vertical: 10, horizontal: 22.5),
    decoration: BoxDecoration(
      border: Border.all(color: Color.fromARGB(120, 255, 255, 255)),
      borderRadius: BorderRadius.all(Radius.circular(8)),
    ),
    child: Column(
      children: [
        Text('jgdkfdjf'),
      ],
    )
  );
}
